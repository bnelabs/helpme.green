apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helpme-green-data
  namespace: helpme-green
  labels:
    app.kubernetes.io/name: helpme-green
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 10Gi
